<%= render 'layouts/errors', obj: @diary %>
<div class="container mt-5">
  <h1 class="text-center">日記新規投稿</h1>

  <%= form_with model: @diary, multipart: true, class: "mt-4" do |f| %>
    <div class="form-group">
      <h4>猫ちゃん</h4>
       <%= f.collection_select :cat_id, @cats, :id, :name, prompt: '選択してください', class: "form-control", rows: 3, cols:6 %>
    </div>

    <div class="form-group">
      <h4>画像（複数投稿できます）</h4>
      <%= f.file_field :images, multiple: true, class: "form-control" %>
    </div>

    <div class="form-group">
      <h4>タグ(“/”で複数個登録できます)</h4>
      <%= f.text_field :name, autofocus: true, placeholder: "三毛猫/保護猫/おなやみ   お悩み相談は[おなやみ]タグをつけて投稿してください！ ", class: "form-control" %>
    </div>

    <div class="form-group">
      <h4>タイトル</h4>
      <%= f.text_field :title, placeholder: "こちらがタイトルです。", class: "form-control" %>
    </div>

    <div class="form-group">
      <h4>本文</h4>
      <%= f.text_area :body, placeholder: "こちらが本文です。\r\n猫のことに限らずお好きに日記を書いてみてください！\r\n日記は後から編集・削除できます。", class: "form-control", rows: 15, cols: 50 %>
    </div>

    <div class="form-group">
      <h4>体重</h4>
      <%= f.text_field :weight, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4><尿</h4>
      <%= f.text_field :urine, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4>うんち</h4>
      <%= f.text_field :faces, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4>水分摂取</h4>
      <%= f.text_field :hydration_amount, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4>ごはん</h4>
      <%= f.text_field :food, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4>嘔吐</h4>
      <%= f.text_field :vomiting, placeholder: "例：3.5", autofocus: true, class: "form-control" %>
    </div>
    
    <div class="form-group">
      <h4>天気</h4>
      <%= f.select :weather, Diary.weathers.keys.map { |k| [t("enum.diary.weather.#{k}"), k] }, { include_blank: "指定なし" }, class: "form-control" %>
    </div>
  
    
    <div class="form-group text-center">
      <%= f.submit '投稿', class: "btn btn-success" %>   
    </div>
  <% end %>
  <%= link_to "Back", request.referrer, class: "row d-flex justify-content-center" %>
</div>